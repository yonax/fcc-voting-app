extends layout

block navbar-select
   -var selected = 'My polls'

block content
  ul
    for poll in polls
      li= poll.topic 
        ol
          for choice in poll.choices
            li= choice.text